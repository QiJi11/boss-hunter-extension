# 猎职增强版 MVP TODO

## 已完成

1. 本地 Git 已初始化，当前开发分支为 `ralph/mvp-screening-bugfix`。
2. GitHub SSH 已可认证到 `QiJi11`。
3. `origin` 已配置为 `git@github.com:QiJi11/boss-hunter-extension.git`。
4. 已推送到 GitHub：`main` 和 `ralph/mvp-screening-bugfix` 都已同步到远程仓库。
5. 已修复旧结算页状态泄漏：进入配置页或修改配置后，旧结算不会自动弹回。
6. 已增加显式查看上次结算入口。
7. 已修复重新投递流程：结算页点击重新投递后回到现有岗位列表重选，不直接重新采集。
8. 已修复 V6 投递结果落账：成功、失败、跳过、停止、已建联但未确认发送都进入 `sendResults`。
9. 已完善在线 AI 岗位筛选展示：岗位卡片显示 AI 分数、状态、理由和风险。
10. AI 筛选失败时岗位默认不勾选，并提示人工确认。
11. 已增加本机人工结果反馈闭环：只有已确认送达岗位可手动标记“已回复 / 约面 / 不合适 / 暂无回复”；同岗位覆盖旧标签，可撤销或全量清除。
12. 已增加默认关闭的反馈学习开关：样本至少 5 条时，下一轮 AI 筛选只接收按评分档位汇总的匿名结果计数；公司、HR、聊天、简历、姓名、岗位 ID 和自由备注不会进入新增反馈字段。

## 待做

1. 在真实 BOSS 页面手动验收结算状态、重投流程、AI 筛选展示和投递结果统计。
2. 采集到岗位后，岗位卡片支持查看 JD 详情：优先在面板内展开 JD 摘要，同时提供跳转 BOSS 详情页入口。
3. 首页 AI 对话框暂缓。
4. 招呼语 AI 润色框暂缓到筛选和投递流程稳定之后。
5. 在真实扩展页面手动验收结果反馈按钮的显示、保存、撤销、重开恢复、清空，以及打开学习开关后的下一轮 AI 筛选摘要。

## 验收清单

0. 发布前品牌防回归检查通过：`pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-brand.ps1`。
1. 加载扩展后，BOSS 页面右侧猎职面板正常显示。
2. 能配置简历、城市、岗位并采集岗位。
3. 采集后岗位显示 AI 分数、理由、风险和建议状态。
4. 低分岗位默认不勾选，高分岗位默认勾选。
5. 修改配置后旧结算页不再自动弹回。
6. 点击查看上次结算可以显式打开上一轮结果。
7. 点击重新投递进入岗位列表重选，不进入收集配置页，不触发重新采集。
8. 投递结束后结算数量和明细准确，不出现有处理岗位但 `0成功0失败`。
9. 未登录、验证码、安全验证、AI 失败时明确停止或提示。
10. 无硬编码 API Key。
