# 即投增强版 MVP TODO

## 本轮优先级

1. 修复结算页状态泄漏：修改岗位、城市、行业、筛选条件后，不再自动跳回上一轮结算页。
2. 修复重新投递流程：结算页点击重新投递后进入岗位列表重选，不直接重新采集，也不回配置页。
3. 修复投递结果落账：成功、失败、跳过、已建联未确认发送都必须进入结果数组，避免出现实际处理过但显示 0 成功 0 失败。
4. 完善在线 AI 岗位筛选：采集后展示 AI 分数、理由、风险，低分默认不勾选，高分默认勾选。
5. 保持密钥安全：apiKey 只存 chrome.storage.local，不写死到代码。

## 暂缓

1. 首页 AI 对话框暂缓。
2. 招呼语 AI 润色框暂缓到筛选和投递流程稳定之后。
3. GitHub 远程仓库等 gh 可用或完成网页登录后再推送。

## 验收

1. 加载扩展后 BOSS 页面右侧即投面板正常显示。
2. 能配置简历、城市、岗位并采集岗位。
3. AI 筛选结果能展示，默认勾选符合阈值。
4. 修改配置后旧结算页不再自动弹回。
5. 重新投递进入岗位列表重选。
6. 投递结束后结算数量和明细准确。
