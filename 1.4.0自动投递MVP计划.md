# 猎职插件 1.4.0 自动投递 MVP 计划

> 创建：2026-08-13。状态：进行中（M0 完成）。
> 主任务：为猎职插件实现"自动投递 MVP"。完成后回接求职执行 TODO。

## 目标

自动采集岗位 → 自动过滤 → 自动评分 → 自动生成岗位级招呼语 → 自动沟通 → 自动核验结果 → 人工处理回复 → 用真实结果优化下一批。

保留三种模式：
- 自动模式：用户确认整批后，插件自动串行投递。
- 复核模式：高风险/低置信度岗位逐岗确认。
- 只采集模式：完全不发送。

## 已确认决策

- D1-A：SW 层新增 `startAutoRun` 批处理器，内部串行复用 stage1/stage2。
- D2：岗位级招呼语 `state.greetings[jobId] = {text, variant, sha256, generatedAt}`。
- D3：MVP 排序公式只实现 applyScore + 薪资适配 + 城市白名单，其余 3 项（competitionScore/新鲜度/HR活跃）占位默认 0。
- D4：默认每批 10、每日 30（可配置）、连续失败 3、连续不确定 2、同公司 2、同 HR 1；硬上限 DAILY_SEND_LIMIT=150 不破。
- D5：一次确认整批 + 完整预览面板（采集数/自动投/复核/排除/城市分布/每日剩余/阈值/招呼语示例）。
- 并发固定 1（MAX_SEND_WORKERS 自动批路径强制 1）。
- START_AUTO_RUN 为新增消息；不动已封禁的 START_SEND。
- 不绕过平台验证，不做反检测。
- uncertain 不自动重试；只有平台聊天证据成立才标记 delivered。
- 图片简历默认关闭；整批启动前可明确开启，但每次发送核对选中简历文件哈希。

## 里程碑

### M0 冻结基线（已完成）
- [x] 备份 `dist/liezhi-mvp-v1.3.11.zip` → `backup/v1.3.11-20260813/`（SHA256=6557E1B2357F35F76ADCCC0D9BD31C38C4C55E65CEC17F948657D75E0D107C92）
- [x] 备份 25 个未提交改动文件到 `backup/v1.3.11-20260813/`
- [x] 新分支 `feature/auto-send-v1.4.0`（从 feature/personalized-v1.3.0 HEAD）
- [x] manifest version → 1.4.0
- [ ] 首次 commit：基线版本号

### M1 恢复自动投递入口
- [ ] 新增 `START_AUTO_RUN` 消息（SW），不碰 START_SEND
- [ ] `buildAutoRunPreview()`：冻结 jobIds + 阈值 + 额度 → 生成批次预览
- [ ] popup 批次预览面板（自动/复核/排除分类、城市分布、招呼语示例、每日剩余）
- [ ] 用户一次确认 → `startAutoRun(frozenConfig)`

### M2 安全状态机
- [ ] `startAutoRun` 串行循环：queued→validating→openingJob→initiatingChat→verifying→delivered|alreadyChatted|failed|uncertain|stopped
- [ ] runId（每批）+ attemptId（每岗=runId-jobId）幂等记账；迟到回调丢弃
- [ ] 自动批并发=1（复用 stage1/stage2，worker 数强制 1）
- [ ] 停止/迟到回调：扩展 sendAborted/abortStage1/timedOut 三层防护到批循环
- [ ] 验证码/安全提示 → captcha_paused 暂停整批，不继续
- [ ] uncertain 不自动重试（复用 _v6MissedJobs 语义 → _autoMissed）
- [ ] 失败不无限重试；连续失败 3 / 连续不确定 2 停止本批

### M3 自动筛选排序
- [ ] 采集 job 补 `salaryMidK`（解析 salary 字符串）与 `cityName`（记录城市）
- [ ] 硬过滤补充：学校硬门槛、城市硬排除
- [ ] 阈值判定：applyScore≥75 自动 / 60-74 复核 / <60 跳过（可配置）
- [ ] 排序：applyScore 主导 + 薪资适配 + 城市白名单加权

### M4 岗位级招呼语
- [ ] `generateJobGreeting(job, resumeText)`：每岗独立生成，传真实 jdSamples
- [ ] 改 SW:905 prompt 为"我是2026届 + 岗位重点 + 项目证据"结构
- [ ] 存 `state.greetings[jobId]={text,variant,sha256}`；sanitize 后校验
- [ ] 生成失败/校验失败 → 该岗进人工复核，不发空消息/通用占位语
- [ ] 招呼语含错误姓名/公司/经历 → 拒绝发送

### M5 自动核验与历史账本
- [ ] jobRecords 扩展字段：runId/attemptId/deliveredAt/viewedAt/repliedAt/interviewAt/rejectedAt/noResponseAsOf
- [ ] 五分类核验：delivered / alreadyChatted / failed / uncertain / stopped
- [ ] 只有 delivered 计入投递数和回复率分母
- [ ] 批次统计 = 明细之和（校验）
- [ ] 不依赖最后一批 sendResults；长期 jobRecords 可回溯

### M6 反馈概率
- [ ] 投递时冻结预测（applyScore/城市/话术 hash）→ 存 jobRecords
- [ ] 7 天回复率、14 天约面率、分层统计（城市/岗位族/分档/话术版本）
- [ ] 样本<5 不显示伪精确概率（保守平滑）
- [ ] "今天刚投无回复"不成为负样本

### M7 验收构建
- [ ] 测试页面 + 不发送模式跑通全链路
- [ ] 用户批准真实测试批（上限 3 岗，由主代理挑选对照已投清单）
- [ ] 真实投递后逐条核验聊天记录
- [ ] 构建 1.4.0 包 + 更新 RUNBOOK/验收清单
- [ ] 验证后才逐步提高至每批 10

## 打分监督（开发中持续）

每里程碑末跑一轮真实岗位打分核对（凉热葵 gpt-5.6-terra），输出三列核对：applyScore｜我的审查｜分歧点。分歧点反向调整打分 prompt/阈值/招呼语结构。

## 评审分歧记录（飞轮输入）

| 岗位 | 插件分 | 我的审查 | 分歧点 | 证据 |
|---|---|---|---|---|
| （每轮打分核对后补充） | | | | |

## 完成后回接

1.4.0 验收通过 → 切回 `投简历TODO_20260810.md` 未完成项：
- P0 163 邮箱 28 封逐封核验
- P0 核验通过邮件同步进 applications.json
- P1 DeepSeek/思必驰跟进（先找官方联系人）
- P1 涂鸦 Moka 状态 + cv@tuya.com 核对
- P1 达观面试
- P2 补足最后 2 条有效投递（杭州 Python/AI 后端 exact）
- P2.1 BOSS 旧候选复核（信创/普实/华为苏州）
- P3 2026-08-14 跟进最匹配 8-10 岗
