# M7e 采集抽查改进点清单（2026-08-14）

> 本轮：恢复 chrome 9250 环境 → 插件 Agent 方向批量采集 → 抽查打分/过滤 → 修复 4 类问题。
> 触发背景：上一会话因 chrome 9250 卡死中断；本次重启后环境干净，chrome 151.0.7922.138 一次启动成功。

## 已修复（4 项，5 个提交）

### 1. AI 打分结果持久化丢失（auto 队列假性为 0）
- **现象**：采集完成、`aiScreeningProgress.done` 显示 64/64，但 `sw:jobs` 里所有 job 无 `aiScreen`，SW 休眠重启后打分全丢 → `buildAutoRunPreview` 全部按 applyScore=0 进 skip/rejected，auto 恒为 0。
- **根因**：打分结果靠 `pushState()`→`persistState()` 的 **500ms debounce** 落盘；SW 打分完成后被 Chrome 空闲终止时，jobs 未落盘。而续筛逻辑直接信任恢复的 `aiScreeningProgress.done`，`for (start=done; ...)` 直接跳过全部不再打分。
- **修复**（commit `e18b33a`）：
  1. 打分完成后立即 `chrome.storage.local.set({ 'sw:jobs': jobs })`，不依赖 debounce；
  2. 续筛点 `done` 以"当前 jobs 中实际已有 `aiScreen` 的岗位数"为上限收敛，进度声称完成但结果缺失时自动从头重打。
- **验证**：修复后重新采集，storage jobs 58 条全部带 `aiScreen`；auto 从假性 0 → 真实 5-6。

### 2. 排除词误拦真实开发岗（两处子问题）
- **2a. AI 评分理由被当排除词命中**（commit `470a682`）：`findExcludeKeywordHit` 的 haystack 包含 `job.aiScreen.reason` 和 `aiScreen.risks`，AI 在 risks 里写"客服运营""算法实现"等作为风险提示，被排除词"运营/算法"整体命中 → 奥达思(94)/燧空(91)/美腾思(91)/汇隆智域(87) 全部误杀进 rejected。
- **2b. 技术/职责词全文匹配 JD 误杀**（commit `469c3c5`）：排除词统一全文匹配，导致"参与系统测试/算法调优/RAG 系统实施/版本交付"等正常开发岗职责描述被"测试/算法/实施/交付/运营"等词误杀。
- **修复**：排除词分级匹配——**强岗位类型词**（实习/外包/驻场/销售/主播/客服/讲师/兼职/标注/审核/代招/猎头/派遣）全文匹配；**普通词**（算法/全栈/测试/运营/实施/交付/培训/推广及学历经验词）只匹配岗位名/公司/薪资/标签。AI 评分理由与 risks 不再纳入排除词 haystack。
- **验证**：修复后 auto 队列恢复包含今测(94)/汇隆智域(92)/美腾思(88)/燧空(82)。

### 3. 匿名代招/猎头岗位未识别（auto 放行外包岗）
- **现象**："深圳某大型计算机软件公司" AI大模型应用开发工程师 96 分进 auto。
- **根因**：`detectCompanyRisk` 只查岗位名/公司名/标签；该岗的"代招公司/人力资源服务许可证/劳务派遣经营许可证/猎头顾问"信息在 BOSS 页面底部资质区，**插件采集的 desc 不含这部分**（desc 仅 393 字符，是 JD 正文）。
- **修复**（commit `3376dbe` + `b4e5b09`）：
  1. `detectCompanyRisk` 增加 JD 正文代招特征词（代招/猎头顾问/劳务派遣经营许可证/人力资源服务许可证/代招公司/猎头招聘/劳务派遣）；
  2. 兜底：匿名公司名正则 `某大型|某知名|某人工智能公司|某上市|某互联网|代招公司|某公司` 识别为代招/外包。
- **验证**：修复后该公司 96 分岗被剔除，auto 队列纯净为实名岗。

## 剩余改进点（未修，留作后续）

### 4. 采集字段缺口：BOSS 资质区信息不入 desc
- 代招/劳务派遣资质、公司认证信息在 BOSS 详情页底部，插件 `job-collector` 只采 JD 正文。建议在采集时把页面底部"认证资质/代招公司/公司工商信息"并入 `job.detail` 或独立字段，让 `detectCompanyRisk` 有完整输入，减少对匿名公司名正则的依赖。

### 5. AI 打分偶发波动
- 同一岗位多轮打分出现 score=0 / 88 / 94 波动（奥达思本轮 score=0，前轮 94）。建议打分失败/低分时记录 `aiScreen.failed` 并提示人工复核，避免因单次 API 抖动把合格岗漏进 skip。

### 6. 应届口径依赖人工确认
- auto 岗中今测/汇隆智域/燧空/美腾思"经验不限"均未写明确应届，阿里云为社招岗。插件无"应届口径"字段；建议采集时识别 JD 中"2026届/应届/毕业时间"并作为打分输入，投递前仍须人工确认。

### 7. SW 休眠与 CDP 轮询可靠性
- 采集/打分长任务中 SW 反复休眠，外部 `collect-status.mjs`/打分脚本频繁遇 `SW_DOWN`，需先导航 BOSS 页唤醒。建议把"唤醒→操作"封装进工具脚本，或给扩展增加可配置的 keepalive 窗口。

## 本轮抽查结论（杭州 Agent 方向 66 岗）

- 采集 66 岗，打分 66/66，完整硬过滤后 **auto=5**（实名、应届口径未冲突、方向匹配）：
  - 今测智能·Agent 工程师 94（大专以上，8-13K 偏低）
  - 杭州汇隆智域·AI Agent应用开发 92（公司成立 3 个月，风险高）
  - 阿里云·Agent 平台数据智能研发 88（社招岗，应届需确认）
  - 美腾思智能·AI Agent工程师 88（学历不限，方向匹配）
  - 燧空智能·AI智能体应用开发 82（8-13K 偏低）
- 误杀/误放已全部修复：匿名代招 96 分剔除、4 个高分开发岗回到 auto。

## 验证记录

- 4 个修复提交后均 `npm test` 全绿（含新增回归用例）。
- 每轮修复后重建 `liezhi-nokey` 副本并 `Extensions.loadUnpacked` 重载，重新采集验证。
- 本次未做任何真实投递；auto 队列岗位如需投递须按 M7b 流程逐岗人工确认。
