# 猎职扩展 v1.3.0 MVP 真机验收清单

**日期：** 2026-08-07
**分支：** `feature/personalized-v1.3.0`（已含 v1.3.0 逐岗复核 + JD 展开 + AI 助手 + 招呼语润色）
**测试基线：** `node tests/run-tests.js` 全绿；全量 `node --check` 通过

> 真机验收需在真实 BOSS 直聘页面 + 已登录 + 已配置 AI 的环境执行。
> 前置：Chrome 加载 `load-extension`（开发模式），或打包 .crx 后安装。

---

## 0. 加载与环境准备

- [ ] Chrome `chrome://extensions` 开启开发者模式 → 加载已解压扩展（本目录）
- [ ] 侧边面板「猎职」可打开，标题显示 v1.3.0
- [ ] 完整设置（齿轮）：配置 OpenAI-compatible Provider / Base URL / API Key / Model，点「测试」通过
- [ ] 粘贴文字简历（AI 筛选/润色依赖）；可选上传图片版简历

---

## 1. 首页 AI 求职助手（P1 · 新增）

- [ ] settings 页顶部「AI 求职助手」可展开/收起
- [ ] 输入问题（如"帮我分析简历适合投什么 AI 岗位"）回车发送
- [ ] 回复基于简历 + 当前筛选配置，多轮对话可追问
- [ ] 未配置 AI 时给出明确错误提示（不会崩溃）

## 2. 岗位采集 + AI 筛选

- [ ] 配置目标城市/职位/行业/学历/年限 → 点「开始采集」
- [ ] 采集后自动运行 AI 筛选（开关 `采集后自动运行 AI 岗位筛选` 默认开）
- [ ] 岗位卡片显示 AI 分数、理由、风险；低分默认不勾选，高分默认勾选
- [ ] 排除关键词新增项生效：实习/博士/硕士及以上/5年以上/3-5年

## 3. 岗位卡片 JD 详情（P0 · 新增）

- [ ] 有 JD 的岗位卡片显示摘要；JD 超过 160 字符出现「展开」按钮
- [ ] 点「展开」显示完整 JD，再点「收起」折叠
- [ ] 点「查看 BOSS 详情 ↗」新标签页打开 `zhipin.com/job_detail/{id}.html`
- [ ] 展开/跳转**不会**误触发岗位勾选（checkbox 状态不变）

## 4. 逐岗复核 + 招呼语润色（P2 · 新增）

- [ ] 勾选岗位 → 点「逐岗复核」弹出确认层
- [ ] 弹层显示：当前岗位 / AI 分数理由风险 / 待发送招呼语 / 简历 / 历史沟通
- [ ] 招呼语为空时「确认并沟通」置灰；「跳过该岗」可跳过
- [ ] **AI 润色**：输入"更活泼些"点「AI 润色」→ 招呼语更新；确认发送用润色后的内容
- [ ] 润色失败（网络/未配 AI）显示错误，不崩溃

## 5. 投递结算 + 状态回归

- [ ] 投递完成后结算页数量和明细准确（成功/失败；`alreadyChatted`/`skipped` 不计入失败）
- [ ] 已建联未确认的岗位显示「状态不确定，请逐岗复核」提示（无一键补发按钮）
- [ ] 修改配置后旧结算页不自动弹回；「查看上次结算」可显式打开
- [ ] 重新投递回到岗位列表重选，不重新采集

## 6. 配置备份与敏感导出

- [ ] 「导出配置」导出不含密钥；「导出含密钥和简历」导出完整快照
- [ ] 导入完整快照可恢复 AI 配置 + 简历 + 图片

## 7. 发布前品牌防回归

- [ ] `pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\check-brand.ps1` 通过
- [ ] 无硬编码 API Key

---

## 已知限制 / 非目标

- 真机自动化（无头跑 BOSS）未做，以上需人工逐条确认
- 招呼语润色仅存在于逐岗复核弹层；组级招呼语列表仍是「重新生成」（REGENERATE），未接入润色
- 首页 AI 对话框为最简版：无会话持久化（关闭弹层即清），无流式输出
- 依赖 AI 接口可达性与配额；429/超时按错误提示处理
